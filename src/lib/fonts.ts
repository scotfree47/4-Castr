{/*

// Current Selection
      import { Cousine } from 'next/font/google'

      export const inter = Cousine({
        subsets: ['latin'],
        weight: ["400", '700'],
        display: 'swap',
        variable: '--font-cousine',
      })

*/}
{/**/}

// Current Selection 2
    import { Fragment_Mono } from 'next/font/google'

    export const inter = Fragment_Mono({
      subsets: ['latin'],
      weight: "400",
      display: 'swap',
      variable: '--font-fragment-mono',
    })

{/**/}

{/*

// Template Original
    import { Inter } from 'next/font/google'

    // Configure Inter font to match exactly what Next.js optimizes for
    export const inter = Inter({
      subsets: ['latin'],
      display: 'swap',
      variable: '--font-inter',
    })

*/}





{/*

// Other Google Fonts

    // Source Code Pro
      import { Source_Code_Pro } from 'next/font/google'

      export const inter = Source_Code_Pro({
        subsets: ['latin'],
        weight: ["400", '500'],
        display: 'swap',
        variable: '--font-source-code-pro',
      })

    // Inconsolata
      import { Inconsolata } from 'next/font/google'

      export const inter = Inconsolata({
        subsets: ['latin'],
        weight: ["400", '500'],
        display: 'swap',
        variable: '--font-inconsolata',
      })


    // Roboto Mono
      import { Roboto_Mono } from 'next/font/google'

      export const inter = Roboto_Mono({
        subsets: ['latin'],
        weight: ["400", '500'],
        display: 'swap',
        variable: '--font-roboto-mono',
      })


    // Space Mono
      import { Space_Mono } from 'next/font/google'

      export const inter = Space_Mono({
        subsets: ['latin'],
        weight: ["400", '700'],
        display: 'swap',
        variable: '--font-space-mono',
      })


    // IBM Plex Mono
      import { IBM_Plex_Mono } from 'next/font/google'

      export const inter = IBM_Plex_Mono({
        subsets: ['latin'],
        weight: ["400", '500'],
        display: 'swap',
        variable: '--font-ibm-plex-mono',
      })


    // B612 Mono
      import { B612_Mono } from 'next/font/google'

      export const inter = B612_Mono({
        subsets: ['latin'],
        weight: ["400", '700'],
        display: 'swap',
        variable: '--font-b612-mono',
      })

    Cousine
    // Cousine
      import { Cousine } from 'next/font/google'

      export const inter = Cousine({
        subsets: ['latin'],
        weight: ["400", '700'],
        display: 'swap',
        variable: '--font-cousine',
      })

    // PT Mono
      import { PT_Mono } from 'next/font/google'

      export const inter = PT_Mono({
        subsets: ['latin'],
        weight: "400",
        display: 'swap',
        variable: '--font-pt-mono',
      })


// Template
  import { Font_Name } from 'next/font/google'

  export const inter = Font_Name({
    subsets: ['latin'],
    weight: ["400", '500'],
    display: 'swap',
    variable: '--font-font-name',
  })

*/}